import React from 'react'


class UserAuthentication extends React.Component {
    // Responsible for displaying the Project Ideas.
    constructor(props) {
        super(props);
    }

    componentDidMount() {
        // Check if there is a refresh_token in local storage, and if so,
        // 
    }

    refreshAccessToken = () => {
        // Method to refresh the access token, using the refresh token
    }

    render() { return (null) }
}

export default UserAuthentication